name: lifecycle_guard_platform_interface
description: "Platform interface for lifecycle_guard - defines the contract for background execution."
version: 1.0.2
repository: https://github.com/Crealify/lifecycle_guard.git
homepage: https://anil-bhattarai.com.np

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
